# chap3lab-task5

The target web application implements a login rate-limiting mechanism intended to block brute-force attacks. However, the protection mechanism contains a logic flaw – a successful login resets the failure counter.

Your task is to modify the provided attack script to bypass the protection and discover the victim's password.
